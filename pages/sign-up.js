import Head from 'next/head'
import { render } from 'react-dom';

export default SignUp = () => {
    render(
        <div className="container">
            <Head>
                <title>Sign Up Kanji List</title>
                <link rel="icon" href="/favicon.ico" />
            </Head>
        </div>
    );
};
